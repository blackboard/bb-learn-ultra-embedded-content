const MESSAGE_TYPES = {
    incoming: {
        initContent: 'init_content',
    },
    outgoing: {
        pageLoaded: 'page_loaded',
        canceled: 'content_canceled',
        contentReady: 'content_ready',
    },
};

export default MESSAGE_TYPES;
